import { styled } from '@mui/material/styles';

export const ErrorComponentContainer = styled('div')({
  width: '100%',
  maxWidth: 500,
});

export const ErrorComponentContent = styled('div')({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
}); 